This repository contains an AI Chatbot built using Python.
The chatbot uses an external AI API (for example: OpenAI or any other AI provider) to generate intelligent responses.

⚠️ Important:
For security reasons, the API key is NOT included in this repository.
Each user must create their own .env file and add their own AI API key.
